---
phase: COURTMATE-02-venue-booking-match-control-center
plan: "02-02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/app/(tabs)/chat.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
must_haves:
  truths:
    - "D-05: Giao diện Segmented Control cho phép chuyển đổi Tab Lịch hẹn/Hộp chat nhóm mượt mà, đồng bộ"
    - "D-06: Khi đặt sân thành công ở booking.tsx, phòng chat tương ứng xuất hiện tự động trong Hộp thư Chat"
    - "D-07: Khung chat có hai nút Quick Action: 'Vị trí đồng đội' và 'Thông tin sân đặt' hiển thị Modal chứa thông tin chi tiết chính xác của sân/đồng đội"
  artifacts:
    - path: "frontend/app/(tabs)/chat.tsx"
      provides: "Giao diện Lịch trình & Hộp chat nhóm với Quick Actions"
      contains: "activeTab"
---

<objective>
Thiết kế Giao diện tab đôi (Segmented Control) Lịch hẹn & Hộp chat nhóm (Chat Screen) kèm các nút thao tác nhanh: xem vị trí đồng đội thực tế trên bản đồ và hóa đơn chi tiết sân đã đặt.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/COURTMATE-02-venue-booking-match-control-center/02-CONTEXT.md
@.planning/phases/COURTMATE-02-venue-booking-match-control-center/02-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Tối ưu hóa Segmented Control và Đồng bộ Lịch hẹn & Chat</name>
  <files>frontend/app/(tabs)/chat.tsx</files>
  <read_first>frontend/app/(tabs)/chat.tsx, frontend/hooks/useMockData.tsx</read_first>
  <action>
    - Đảm bảo thanh chuyển đổi tab đôi (Lịch hẹn / Hộp thư chat) hoạt động chính xác bằng cách đồng bộ `activeTab` state.
    - Cải thiện hiển thị danh sách Lịch hẹn sắp đấu. Đồng bộ hóa trực tiếp từ hook `userMatches`.
    - Đồng bộ hóa danh sách các cuộc hội thoại từ hook `activeConversations`. Khi người dùng đặt sân thành công bên màn hình Booking, cuộc hội thoại mới với "Tiếp tân đại lý" sẽ tự động hiển thị trong Hộp thư chat và lịch đặt sân hiển thị trong tab Lịch hẹn.
  </action>
  <verify>Chạy kiểm tra cú pháp TypeScript: npx tsc --noEmit trong thư mục frontend</verify>
  <acceptance_criteria>
    - chat.tsx hiển thị chính xác các đặt sân mới được thêm từ booking.tsx.
    - Chuyển đổi tab không bị trễ hay giật lag UI.
  </acceptance_criteria>
  <done>Đồng bộ dữ liệu Lịch trình & Hộp thư Chat hoạt động thông suốt.</done>
</task>

<task type="auto">
  <name>Nâng cấp Popover Modal Vị trí đồng đội và Thông tin sân đặt</name>
  <files>frontend/app/(tabs)/chat.tsx</files>
  <read_first>frontend/app/(tabs)/chat.tsx</read_first>
  <action>
    - Cập nhật Modal "Vị trí đồng đội": Tối ưu thiết kế bản đồ giả lập di động, hiển thị các pins vị trí của đồng đội cùng khoảng cách (ví dụ 0.6km, 1.1km) sinh động, tạo cảm giác real-time tracking bằng GPS.
    - Cập nhật Modal "Thông tin sân đặt": Hiển thị hình ảnh sân thể thao, địa chỉ chi tiết, khung giờ đặt (Sân số mấy, từ mấy giờ đến mấy giờ) được đồng bộ động từ phòng chat hiện tại (lấy thông tin của active chat).
    - Thêm nút Quick Action "Gọi điện tiếp tân/host" ở header để người dùng liên hệ trực tiếp khi có vấn đề.
  </action>
  <verify>Kiểm tra lỗi biên dịch TypeScript trong frontend</verify>
  <acceptance_criteria>
    - chat.tsx chứa hai nút Quick Action hoạt động chuẩn dưới dạng modal popup.
    - Modal 'Thông tin sân đặt' hiển thị đúng hình ảnh và thông tin của sân đã đặt tương ứng với phòng chat hiện tại.
    - Modal 'Vị trí đồng đội' hiển thị các vị trí pins trực quan.
  </acceptance_criteria>
  <done>Các nút thao tác nhanh (Quick Actions) hoạt động trực quan và hiển thị đầy đủ thông tin.</done>
</task>
</tasks>

<verification>
- [ ] Chạy lệnh `npx tsc --noEmit` trong thư mục `frontend` không báo lỗi.
- [ ] Xác nhận khi bấm vào phòng chat đặt sân, các nút Quick Actions hiển thị thông tin khớp với sân đã đặt.
</verification>

<success_criteria>
- Người dùng chuyển đổi mượt mà giữa Lịch hẹn và Chat, xem được vé check-in QR, xem vị trí đồng đội và thông tin đặt sân trực tiếp từ Quick Actions trong phòng chat.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/COURTMATE-02-venue-booking-match-control-center/02-02-SUMMARY.md`
</output>
