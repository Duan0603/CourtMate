---
phase: COURTMATE-02-venue-booking-match-control-center
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/hooks/useMockData.tsx
  - frontend/app/(tabs)/booking.tsx
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03
must_haves:
  truths:
    - "D-01: Giao diện Đặt sân hiển thị dải lịch ngang 7 ngày ở trên cùng, cho phép chuyển đổi ngày và cập nhật lại trạng thái trống/bận tương ứng"
    - "D-02: Cho phép người dùng chạm chọn nhiều slot giờ liên tiếp (Consecutive Multi-slots) và tự động cộng dồn giá tiền hiển thị dưới Bottom Bar"
    - "D-03: Nếu người dùng chạm chọn slot không liên tiếp, hệ thống sẽ hiển thị cảnh báo Alert/Toast và reset lựa chọn cũ, đặt slot mới làm điểm bắt đầu"
    - "D-04: Các slot giờ cao điểm được đánh dấu trực quan bằng biểu tượng 🔥 bên cạnh giá tiền tương ứng"
  artifacts:
    - path: "frontend/hooks/useMockData.tsx"
      provides: "Dữ liệu mock hỗ trợ nhiều slot, theo ngày và giờ cao điểm"
      contains: "dateString"
    - path: "frontend/app/(tabs)/booking.tsx"
      provides: "Giao diện bộ chọn ngày ngang và lưới slot giờ liên tiếp"
      contains: "getNext7Days"
---

<objective>
Thiết kế chi tiết giao diện Đặt sân (Venue Booking Screen) dạng Card UI tích hợp bộ chọn ngày cuộn ngang 7 ngày, lưới chọn giờ cao điểm 🔥 và logic chọn nhiều slot giờ liên tiếp tối ưu Thumb Zone.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/COURTMATE-02-venue-booking-match-control-center/02-CONTEXT.md
@.planning/phases/COURTMATE-02-venue-booking-match-control-center/02-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Nâng cấp Mock Data trong useMockData.tsx</name>
  <files>frontend/hooks/useMockData.tsx</files>
  <read_first>frontend/hooks/useMockData.tsx</read_first>
  <action>
    Cập nhật hook `useMockData` để quản lý trạng thái đặt sân theo ngày và cho phép đặt nhiều slot liên tiếp cùng lúc.
    - Cấu trúc lại hàm `bookVenueSlot` để nhận tham số danh sách các slot (ví dụ `slotTimes: string[]`) và ngày đặt (`dateString: string`).
    - Lưu lịch sử đặt sân theo ngày và mã sân để tránh việc đặt sân ở ngày này bị hiển thị bận ở ngày khác.
    - Định nghĩa thuộc tính giá động hoặc đánh dấu sẵn giờ cao điểm (Peak Hours) cho các slot thuộc khung giờ từ 17:00 - 21:00 với giá tăng thêm 15-20% trong mock data gốc ở `frontend/constants/MockData.ts`.
  </action>
  <verify>Chạy kiểm tra cú pháp TypeScript: npx tsc --noEmit trong thư mục frontend</verify>
  <acceptance_criteria>
    - useMockData.tsx chứa hàm bookVenueSlot cập nhật đúng danh sách slot bận theo ngày đã chọn.
    - Dữ liệu lịch hẹn mới (UserMatch) được thêm chính xác với thông tin ngày cụ thể.
  </acceptance_criteria>
  <done>Mock data hook hỗ trợ đầy đủ cho việc đặt lịch nhiều slot theo ngày.</done>
</task>

<task type="auto">
  <name>Thiết kế dải chọn ngày cuộn ngang và nhãn cao điểm 🔥</name>
  <files>frontend/app/(tabs)/booking.tsx</files>
  <read_first>frontend/app/(tabs)/booking.tsx, frontend/constants/MockData.ts</read_first>
  <action>
    - Triển khai hàm `getNext7Days` để lấy danh sách 7 ngày tiếp theo (bắt đầu từ ngày hiện tại).
    - Tạo Component dải lịch cuộn ngang 7 ngày dạng `ScrollView` ở đầu màn hình đặt sân. Mỗi ngày là một nút chứa Thứ và Ngày (ví dụ: T2, 22), làm nổi bật ngày đang hoạt động bằng màu Neon Green (`#39FF14`).
    - Cập nhật Grid ma trận slot giờ. Thêm biểu tượng 🔥 hoặc chữ `Peak` bên cạnh mức giá đối với các slot giờ cao điểm (khung từ 17:00 - 21:00).
  </action>
  <verify>Kiểm tra lỗi biên dịch TypeScript trong frontend</verify>
  <acceptance_criteria>
    - booking.tsx chứa bộ cuộn dải lịch ngang 7 ngày.
    - Khi thay đổi ngày, lưới slot giờ tương ứng cập nhật lại trạng thái trống/bận chính xác.
    - Lưới chọn slot giờ hiển thị nhãn/icon 🔥 cho giờ cao điểm.
  </acceptance_criteria>
  <done>Giao diện dải lịch ngang 7 ngày và nhãn cao điểm hoạt động hoàn hảo.</done>
</task>

<task type="auto">
  <name>Triển khai logic chọn nhiều slot giờ liên tiếp và tính tổng tiền</name>
  <files>frontend/app/(tabs)/booking.tsx</files>
  <read_first>frontend/app/(tabs)/booking.tsx, frontend/hooks/useMockData.tsx</read_first>
  <action>
    - Thay thế state `selectedSlot` (đơn lẻ) bằng `selectedSlots` (mảng các slot đã chọn).
    - Cài đặt hàm `handleSlotSelect` thực thi logic:
      - Chỉ cho phép chọn các slot liền kề nhau về mặt thời gian.
      - Nếu người dùng chọn slot không liên tiếp, hiển thị `Alert.alert('Thông báo', 'Vui lòng chọn các khung giờ liền kề nhau.')`, đồng thời reset (xóa toàn bộ các slot đang chọn) và đặt slot mới chọn làm slot bắt đầu mới.
    - Cập nhật hiển thị tổng tiền ở Bottom Bar bằng cách cộng dồn `price` của tất cả các slot đang chọn trong `selectedSlots`.
    - Khi ấn "Đặt sân ngay" và xác nhận thanh toán, truyền toàn bộ danh sách slot đã chọn qua hàm `bookVenueSlot`.
  </action>
  <verify>Kiểm tra lỗi biên dịch TypeScript trong frontend</verify>
  <acceptance_criteria>
    - Người dùng có thể chọn được nhiều slot giờ liên tiếp cùng lúc.
    - Alert hiển thị khi chọn slot rời rạc và reset danh sách chọn thành slot mới.
    - Bottom Bar hiển thị tổng tiền cộng dồn chính xác.
  </acceptance_criteria>
  <done>Logic chọn nhiều slot liên tiếp và thanh toán hóa đơn hoạt động chuẩn xác.</done>
</task>
</tasks>

<verification>
- [ ] Chạy lệnh `npx tsc --noEmit` trong thư mục `frontend` không báo lỗi.
- [ ] Xác nhận giao diện dải chọn ngày cuộn ngang 7 ngày hiển thị chuẩn xác Thứ/Gary và phản hồi chạm mượt mà.
- [ ] Xác nhận cảnh báo lỗi xuất hiện khi chọn slot rời rạc.
</verification>

<success_criteria>
- Người dùng đặt được nhiều slot liên tiếp thành công, hóa đơn checkout hiển thị chính xác danh sách giờ đã đặt và tổng số tiền thanh toán.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/COURTMATE-02-venue-booking-match-control-center/02-01-SUMMARY.md`
</output>
