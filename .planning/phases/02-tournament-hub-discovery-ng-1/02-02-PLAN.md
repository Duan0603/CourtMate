---
phase: 02-tournament-hub-discovery-ng-1
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/frontend/src/features/tournaments/components/TournamentCard.tsx
  - apps/frontend/src/features/tournaments/screens/TournamentHubScreen.tsx
  - apps/frontend/src/features/tournaments/screens/TournamentDetailScreen.tsx
autonomous: true
requirements:
  - TOUR-01
  - TOUR-02
must_haves:
  truths:
    - "D-09/D-10: Large image cards with Name, Date, Sport, Location, Fee"
    - "D-19: Tabs (Info, Rules, Schedule, Participants) on detail page"
    - "D-20: Sticky bottom bar with 'Register' CTA"
---

<objective>
Implement responsive tournament list and detail screens in the frontend app.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tournament-hub-discovery-ng-1/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TournamentCard Component</name>
  <files>apps/frontend/src/features/tournaments/components/TournamentCard.tsx</files>
  <action>Create TournamentCard component using Tamagui. Include large cover image, Registration Status badge, Bookmark placeholder icon, Title, Date, Location, and 'From [X] VND' fee text.</action>
  <verify>Component compiles cleanly</verify>
  <acceptance_criteria>
    - Renders as a large image card with all required information overlays
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Tournament Hub Screen</name>
  <files>apps/frontend/src/features/tournaments/screens/TournamentHubScreen.tsx</files>
  <action>Build main feed screen fetching from GET /tournaments. Implement pull-to-refresh, horizontal sport tab filter, and empty state fallback (national view with 'Other Cities' header, Change City sheet, 'Coming Soon' toasts for Create and Notify Me).</action>
  <verify>Screen compiles cleanly</verify>
  <acceptance_criteria>
    - Correctly displays fallback empty state when city has no tournaments
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement Tournament Detail Screen</name>
  <files>apps/frontend/src/features/tournaments/screens/TournamentDetailScreen.tsx</files>
  <action>Build detail page fetching from GET /tournaments/:id. Implement navigation tabs (Info, Rules, Schedule, Participants) and sticky bottom bar with fee and Register CTA (shows 'opening soon' toast).</action>
  <verify>Screen compiles cleanly</verify>
  <acceptance_criteria>
    - Screen is split into 4 tabs and includes the sticky register bar
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds for frontend
- [ ] Screens render without runtime errors
</verification>

<success_criteria>
- Frontend matches the detailed decisions from CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/02-tournament-hub-discovery-ng-1/02-02-SUMMARY.md`
</output>
