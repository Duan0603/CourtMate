---
phase: 02-tournament-hub-discovery-ng-1
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/index.ts
  - apps/backend/src/modules/tournaments/tournament.schema.ts
  - apps/backend/src/modules/tournaments/tournaments.service.ts
  - apps/backend/src/modules/tournaments/tournaments.controller.ts
  - apps/backend/src/modules/tournaments/tournaments.module.ts
  - apps/backend/src/app.module.ts
autonomous: true
requirements:
  - TOUR-01
  - TOUR-02
must_haves:
  truths:
    - "D-15: Default chronological order by upcoming date"
    - "D-16: Prioritize Open for Registration status"
    - "D-01/D-02: Support fetching by city, and fallback to national feed if local is empty"
  artifacts:
    - path: "apps/backend/src/modules/tournaments/tournament.schema.ts"
      provides: "Mongoose schema for tournaments"
---

<objective>
Build database schema and API endpoints for fetching and listing tournaments.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tournament-hub-discovery-ng-1/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Tournament Types in Shared Package</name>
  <files>packages/shared/src/index.ts</files>
  <action>Add Tournament, TournamentStatus, Category types to packages/shared/src/index.ts. Status should include Upcoming, Open, Full, InProgress, Completed. Rebuild shared package.</action>
  <verify>npm run build in packages/shared passes</verify>
  <acceptance_criteria>
    - packages/shared/src/index.ts contains Tournament and TournamentStatus exports
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Tournaments Module and Mongoose Schema</name>
  <files>apps/backend/src/modules/tournaments/tournament.schema.ts, apps/backend/src/modules/tournaments/tournaments.service.ts, apps/backend/src/modules/tournaments/tournaments.controller.ts, apps/backend/src/modules/tournaments/tournaments.module.ts, apps/backend/src/app.module.ts</files>
  <action>Create Mongoose schema mapping to the shared Tournament type. Register the module in app.module.ts. Service should have findAll (with city filtering and sorting logic) and findById methods.</action>
  <verify>tsc --noEmit inside apps/backend passes</verify>
  <acceptance_criteria>
    - API exposes GET /tournaments and GET /tournaments/:id
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Seed Mock Data</name>
  <files>apps/backend/src/modules/tournaments/tournaments.service.ts</files>
  <action>Add an onModuleInit hook in TournamentsService to seed mock tournaments if the collection is empty, providing enough data to test national fallback and sorting.</action>
  <verify>API responds with mock data on GET /tournaments</verify>
  <acceptance_criteria>
    - Mock data is inserted on startup
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds for backend
- [ ] GET /tournaments responds with 200 and tournament list
</verification>

<success_criteria>
- Backend endpoints return tournament data matching Phase 2 UI requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tournament-hub-discovery-ng-1/02-01-SUMMARY.md`
</output>
