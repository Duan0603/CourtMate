---
phase: COURTMATE-01-foundation-on-demand-matchmaking
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/(tabs)/index.tsx
  - components/Home/GPSHeader.tsx
  - components/Home/SportScroll.tsx
  - components/Home/MatchmakerCTA.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - DSGN-01
  - DSGN-02
must_haves:
  truths:
    - "D-05: Trang chủ thiết kế Dark Mode làm chủ đạo với các màu nhấn Neon Green (#39FF14)"
    - "D-06: Nút TÌM ĐỒNG ĐỘI NGAY thiết kế lớn trong Thumb Zone ở vị trí trung tâm dưới màn hình để dễ chạm 1 tay"
    - "Trang chủ hiển thị định vị GPS giả lập và thanh tìm kiếm nhanh"
    - "Danh sách biểu tượng môn thể thao cuộn ngang hoạt động mượt mà"
  artifacts:
    - path: "components/Home/GPSHeader.tsx"
      provides: "GPS Location indicator showing mock city and mock search input header"
      contains: "search"
    - path: "components/Home/SportScroll.tsx"
      provides: "Horizontal scrollable list of sport items with icons and selective states"
      contains: "ScrollView"
    - path: "components/Home/MatchmakerCTA.tsx"
      provides: "Large CTA button optimized for thumb placement that triggers navigation to Map tab"
      contains: "TÌM ĐỒNG ĐỘI NGAY"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "components/Home/GPSHeader.tsx"
      via: "Render GPSHeader component"
    - from: "app/(tabs)/index.tsx"
      to: "components/Home/SportScroll.tsx"
      via: "Render SportScroll component"
    - from: "app/(tabs)/index.tsx"
      to: "components/Home/MatchmakerCTA.tsx"
      via: "Render MatchmakerCTA component"
---

<objective>
Thiết kế giao diện Trang chủ di động (Home Screen) chứa GPS Location Header giả lập, thanh tìm kiếm nhanh, thanh cuộn ngang các môn thể thao (Sport Horizontal Scroll), và nút CTA "TÌM ĐỒNG ĐỘI NGAY" lớn trong Thumb Zone.

Purpose: Xây dựng màn hình tương tác chính đầu tiên của người dùng, làm cổng kết nối để chuyển hướng sang luồng tìm bạn (Matchmaking).
Output: Màn hình Home Screen hoàn thiện giao diện mẫu thử tương tác động trên client.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-CONTEXT.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-UI-SPEC.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-RESEARCH.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Thiết kế GPS Header & Search Bar</name>
  <files>components/Home/GPSHeader.tsx</files>
  <read_first>constants/Colors.ts</read_first>
  <action>
    Tạo component `components/Home/GPSHeader.tsx` hiển thị thông tin GPS vị trí hiện tại giả lập (ví dụ: "Sân vận động Mỹ Đình, Hà Nội" hoặc "Quận 1, TP. Hồ Chí Minh") sử dụng SafeArea insets để không bị tai thỏ che khuất.
    Tích hợp thanh tìm kiếm nhanh (Search Bar) bên dưới GPS indicator với giao diện tối giản, bo tròn và viền sáng tinh tế.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - GPSHeader sử dụng các màu từ constants/Colors.ts
    - Thiết kế input text nhập được và có icon Search đi kèm
  </acceptance_criteria>
  <done>Component GPSHeader hoàn thành, hiển thị đúng SafeArea và giao diện tối.</done>
</task>

<task type="auto">
  <name>Thiết kế Thanh cuộn môn thể thao (Sport Horizontal Scroll)</name>
  <files>components/Home/SportScroll.tsx</files>
  <read_first>components/Home/GPSHeader.tsx</read_first>
  <action>
    Tạo component `components/Home/SportScroll.tsx` chứa danh sách các môn thể thao: Cầu lông, Bóng rổ, Bóng đá, Quần vợt, Bóng bàn.
    Sử dụng `ScrollView` với thuộc tính `horizontal={true}` và `showsHorizontalScrollIndicator={false}` để cho phép cuộn ngang mượt mà.
    Mỗi môn thể thao là một nút bấm (Card/Badge) có icon lucide tương ứng. Khi chạm vào sẽ thay đổi trạng thái (active/inactive) và chuyển đổi màu sắc (màu Neon Green cho trạng thái active).
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - SportScroll chứa ScrollView cuộn ngang
    - Có quản lý trạng thái active môn thể thao được chọn
  </acceptance_criteria>
  <done>Thanh chọn môn thể thao cuộn ngang tương tác tốt.</done>
</task>

<task type="auto">
  <name>Thiết kế nút CTA "TÌM ĐỒNG ĐỘI NGAY" và tích hợp Home Screen</name>
  <files>components/Home/MatchmakerCTA.tsx, app/(tabs)/index.tsx</files>
  <read_first>components/Home/SportScroll.tsx</read_first>
  <action>
    Tạo component `components/Home/MatchmakerCTA.tsx` hiển thị nút CTA lớn màu nền Neon Green (#39FF14) nổi bật trên nền tối.
    Đặt nút ở vị trí tối ưu hóa cho tầm với của ngón cái (Thumb Zone) — cách đáy màn hình hợp lý.
    Bấm nút này sẽ sử dụng router của `expo-router` để chuyển hướng sang màn hình tìm bạn (tab `map`).
    Tích hợp 3 components trên vào Trang chủ `app/(tabs)/index.tsx` để tạo thành giao diện Dashboard Trang chủ đồng bộ.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - app/(tabs)/index.tsx kết hợp và render GPSHeader, SportScroll và MatchmakerCTA đúng thứ tự bố cục
    - MatchmakerCTA chuyển tab điều hướng qua '/map' bằng `useRouter` của expo-router
  </acceptance_criteria>
  <done>Màn hình Trang chủ tích hợp hoàn chỉnh và hoạt động điều hướng tab trơn tru.</done>
</task>
</tasks>

<verification>
- [ ] Lệnh `npx tsc --noEmit` chạy thành công không có lỗi kiểu dữ liệu.
</verification>

<success_criteria>
- Trang chủ hiển thị đúng thiết kế Dark Mode, màu nhấn Neon Green cho nút bấm và môn thể thao đã chọn.
- Bấm nút "TÌM ĐỒNG ĐỘI NGAY" lập tức chuyển người dùng sang Tab Bản đồ tìm bạn chơi.
</success_criteria>

<output>
After completion, create `.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-02-SUMMARY.md`
</output>
