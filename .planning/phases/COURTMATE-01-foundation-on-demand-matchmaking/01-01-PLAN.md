---
phase: COURTMATE-01-foundation-on-demand-matchmaking
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - package.json
  - app.json
  - app/_layout.tsx
  - app/(tabs)/_layout.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/map.tsx
  - app/(tabs)/schedule.tsx
  - app/(tabs)/profile.tsx
  - constants/Colors.ts
autonomous: true
requirements:
  - DSGN-01
  - DSGN-03
must_haves:
  truths:
    - "D-01: TypeScript cấu hình strict (strict: true) và path alias @/* hoạt động chuẩn"
    - "D-02: Ứng dụng React Native/Expo khởi chạy thành công không có lỗi build"
    - "D-03: Định tuyến Expo Router hoạt động đúng với cấu trúc thư mục app/"
    - "D-04: Thanh điều hướng Bottom Navigation Bar hiển thị đầy đủ 4 tab với icon tương ứng"
    - "Người dùng có thể chuyển đổi giữa các tab bằng cách chạm"
  artifacts:
    - path: "tsconfig.json"
      provides: "TypeScript configuration with strict type checking and @/* alias"
      contains: "\"strict\": true"
    - path: "app/_layout.tsx"
      provides: "Root layout wrapping app with GestureHandlerRootView and SafeAreaProvider"
      contains: "GestureHandlerRootView"
    - path: "app/(tabs)/_layout.tsx"
      provides: "Bottom navigation tabs configurations with active active Neon Green indicator"
      contains: "tabBarActiveTintColor"
  key_links:
    - from: "app/_layout.tsx"
      to: "app/(tabs)/_layout.tsx"
      via: "Expo Router Slot or Stack transition"
---

<objective>
Khởi tạo cấu trúc dự án React Native/Expo với TypeScript strict mode, cấu hình định tuyến Expo Router, thiết lập hệ thống Gesture Handler & Safe Area và thiết kế Bottom Navigation Bar cố định gồm 4 tab.

Purpose: Đặt nền móng vững chắc về kiến trúc điều hướng và cấu hình dự án, đảm bảo tuân thủ thiết kế Dark Mode + Neon Green.
Output: Dự án Expo mới cấu hình chuẩn, mã nguồn định tuyến tab bar hoạt động hoàn chỉnh.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-CONTEXT.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-UI-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Khởi tạo dự án Expo & Cấu hình TypeScript Strict</name>
  <files>tsconfig.json, package.json, app.json</files>
  <action>
    Khởi tạo khung dự án Expo mới tại thư mục gốc của workspace sử dụng mẫu Expo Blank TypeScript hoặc cài đặt thủ công.
    Cài đặt các gói phụ thuộc cần thiết:
    `npx expo install expo-router react-native-safe-area-context react-native-screens react-native-gesture-handler expo-constants expo-status-bar`
    Cập nhật `tsconfig.json` để kích hoạt strict type checking (`"strict": true`) và thiết lập path aliases cho `@/*` trỏ về root directory.
    Cập nhật `package.json` và `app.json` để hỗ trợ định tuyến file-system của `expo-router` với điểm bắt đầu là thư mục `app`.
  </action>
  <verify>Chạy kiểm tra cú pháp TypeScript: npx tsc --noEmit</verify>
  <acceptance_criteria>
    - tsconfig.json chứa thuộc tính "strict": true
    - tsconfig.json cấu hình path alias "@/*"
    - app.json khai báo "scheme" và cấu hình cấu trúc expo-router chuẩn
  </acceptance_criteria>
  <done>Dự án được khởi tạo và biên dịch TypeScript thành công không có lỗi.</done>
</task>

<task type="auto">
  <name>Thiết lập Root Layout và Constants bảng màu</name>
  <files>app/_layout.tsx, constants/Colors.ts</files>
  <read_first>.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-UI-SPEC.md</read_first>
  <action>
    Tạo tệp `constants/Colors.ts` định nghĩa bảng màu Dark Mode chủ đạo:
    - Dominant (Background): #121212
    - Secondary (Surface/Cards): #1C1C1E
    - Accent (Highlight/CTA): #39FF14 (Neon Green)
    - Destructive: #FF3B30
    Tạo root layout `app/_layout.tsx` bọc toàn bộ ứng dụng trong `GestureHandlerRootView` (style={ flex: 1 }) và `SafeAreaProvider` để sẵn sàng cho Bottom Sheet và xử lý SafeArea tai thỏ.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - app/_layout.tsx chứa thẻ bọc <GestureHandlerRootView style={{ flex: 1 }}>
    - app/_layout.tsx chứa thẻ bọc <SafeAreaProvider>
    - constants/Colors.ts xuất khẩu bảng màu Dark Mode với các mã màu chuẩn từ UI-SPEC
  </acceptance_criteria>
  <done>Root layout cấu hình hoàn tất với các Provider nền tảng.</done>
</task>

<task type="auto">
  <name>Thiết kế Tab Layout điều hướng đáy (Bottom Tab Navigation)</name>
  <files>app/(tabs)/_layout.tsx, app/(tabs)/index.tsx, app/(tabs)/map.tsx, app/(tabs)/schedule.tsx, app/(tabs)/profile.tsx</files>
  <read_first>app/_layout.tsx</read_first>
  <action>
    Cài đặt thư viện icons: `npm install lucide-react-native`
    Tạo thư mục `app/(tabs)` và tệp cấu hình định tuyến tab `app/(tabs)/_layout.tsx` sử dụng thẻ `<Tabs>`.
    Cấu hình phong cách thiết kế:
    - active color: #39FF14 (Colors.dark.accent)
    - inactive color: #8E8E93
    - tabBarStyle background: #1C1C1E (Colors.dark.secondary)
    - Xóa header mặc định (headerShown: false).
    Tạo 4 màn hình khung (skeleton screens) chứa tiêu đề văn bản cơ bản để chuyển đổi:
    - index.tsx (Trang chủ)
    - map.tsx (Bản đồ tìm kiếm)
    - schedule.tsx (Lịch trình)
    - profile.tsx (Hồ sơ)
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - app/(tabs)/_layout.tsx sử dụng các icons chuẩn tương ứng cho 4 tab từ lucide-react-native
    - 4 tệp màn hình tab tồn tại và xuất khẩn component React mặc định hợp lệ
  </acceptance_criteria>
  <done>Bottom Tab Navigator chuyển màn hình hoạt động mượt mà.</done>
</task>
</tasks>

<verification>
- [ ] Chạy lệnh `npx tsc --noEmit` đạt kết quả biên dịch thành công.
- [ ] Xác nhận cấu hình tệp định tuyến chính xác.
</verification>

<success_criteria>
- Khởi chạy ứng dụng Expo thành công mà không có lỗi thiếu thư viện hay sai kiểu TypeScript.
- Tab bar hiển thị đúng thiết kế Dark Mode và Neon Green.
</success_criteria>

<output>
After completion, create `.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-01-SUMMARY.md`
</output>
