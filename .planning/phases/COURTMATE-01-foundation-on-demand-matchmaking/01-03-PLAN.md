---
phase: COURTMATE-01-foundation-on-demand-matchmaking
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - app/(tabs)/map.tsx
  - components/Map/MatchRadar.tsx
  - components/Map/MatchBottomSheet.tsx
  - hooks/useMockData.ts
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - DSGN-01
  - DSGN-02
must_haves:
  truths:
    - "D-07: Sóng Radar quét lan tỏa tuần hoàn mượt mà bằng react-native-reanimated chạy trên UI Thread"
    - "D-08: Bản đồ hiển thị ở chế độ toàn màn hình sử dụng react-native-maps"
    - "D-09: Tọa độ bản đồ sử dụng dữ liệu giả lập (mock data) từ useMockData"
    - "D-10: Bottom Sheet hiển thị kèo chờ với thông tin chi tiết và nút thao tác một chạm Tham gia/Hủy"
  artifacts:
    - path: "components/Map/MatchRadar.tsx"
      provides: "Animated concentric circles radiating outward using react-native-reanimated for real-time radar look"
      contains: "useAnimatedStyle"
    - path: "components/Map/MatchBottomSheet.tsx"
      provides: "BottomSheet structure using gorhom bottom-sheet containing sport distance level details and Join action"
      contains: "BottomSheet"
    - path: "hooks/useMockData.ts"
      provides: "Hook returning coordinates and info for 3-5 mock sports arenas and active matches"
      contains: "latitude"
  key_links:
    - from: "app/(tabs)/map.tsx"
      to: "components/Map/MatchRadar.tsx"
      via: "Render MatchRadar component layered over MapView"
    - from: "app/(tabs)/map.tsx"
      to: "components/Map/MatchBottomSheet.tsx"
      via: "Render MatchBottomSheet overlay container"
---

<objective>
Thiết kế Màn hình tìm kiếm On-demand (Bản đồ tìm bạn) tích hợp Bản đồ toàn màn hình (`react-native-maps`), hiệu ứng sóng quét Radar thời gian thực (`react-native-reanimated`), và kèo chờ hiển thị qua Bottom Sheet (`@gorhom/bottom-sheet`) với chức năng Tham gia/Hủy 1 chạm.

Purpose: Hoàn thiện luồng trải nghiệm tương tác cốt lõi của ứng dụng (On-demand matchmaking).
Output: Màn hình Map/Matchmaking Screen chạy mượt mà trên client với dữ liệu mock phong phú.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-CONTEXT.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-UI-SPEC.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-RESEARCH.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-01-SUMMARY.md
@.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Tạo Hook Dữ liệu mẫu (useMockData)</name>
  <files>hooks/useMockData.ts</files>
  <read_first>constants/Colors.ts</read_first>
  <action>
    Tạo hook `hooks/useMockData.ts` xuất khẩu dữ liệu mock cho:
    - Vị trí của người dùng hiện tại (ví dụ: Hà Nội, Lat: 21.0285, Lng: 105.8542).
    - Danh sách 3-5 sân thể thao lân cận (Sân cầu lông Duy Hưng, Sân bóng Cầu Giấy,...) với tọa độ sai lệch nhỏ so với người dùng.
    - Trạng thái các kèo chờ tương ứng: Tên môn thể thao, trình độ yêu cầu (Trung bình/Khá), khoảng cách (km), số lượng người hiện có/tối đa, thời gian bắt đầu.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - hooks/useMockData.ts xuất khẩu hook dạng TypeScript chuẩn
    - Mock data chứa đầy đủ tọa độ latitude/longitude cho bản đồ
  </acceptance_criteria>
  <done>Mock data hook hoàn thành, cung cấp cấu trúc dữ liệu đầy đủ.</done>
</task>

<task type="auto">
  <name>Thiết kế Bản đồ và hoạt họa sóng quét Radar</name>
  <files>components/Map/MatchRadar.tsx, app/(tabs)/map.tsx</files>
  <read_first>hooks/useMockData.ts</read_first>
  <action>
    Cài đặt và thiết lập bản đồ toàn màn hình trong `app/(tabs)/map.tsx` sử dụng thư viện `react-native-maps`. Hiển thị vị trí người dùng ở trung tâm và các markers sân thể thao xung quanh lấy từ `useMockData`.
    Xây dựng hiệu ứng quét sóng Radar tròn đồng tâm giãn nở tuần hoàn trong `components/Map/MatchRadar.tsx` bằng `react-native-reanimated` (chạy trên UI Thread). Định vị Radar Ring nằm đè trực tiếp tại tâm vị trí người dùng trên bản đồ.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - app/(tabs)/map.tsx tích hợp <MapView> full screen
    - MatchRadar sử dụng Reanimated với timing và looping để vẽ sóng lan tỏa liên tục
  </acceptance_criteria>
  <done>Hiển thị bản đồ thực kèm sóng Radar hoạt động êm ái.</done>
</task>

<task type="auto">
  <name>Thiết kế Bottom Sheet kèo chờ và tính năng 1 chạm</name>
  <files>components/Map/MatchBottomSheet.tsx, app/(tabs)/map.tsx</files>
  <read_first>components/Map/MatchRadar.tsx</read_first>
  <action>
    Thiết kế Bottom Sheet `components/Map/MatchBottomSheet.tsx` sử dụng `@gorhom/bottom-sheet`.
    Cấu hình snapPoints phù hợp (ví dụ: '18%' để hé lộ thông tin khi thu gọn, '60%' khi mở rộng).
    Nội dung hiển thị chi tiết kèo chờ được chọn: khoảng cách, môn thể thao, trình độ, số lượng slots trống.
    Thiết kế nút CTA 1 chạm:
    - Khi chưa tham gia: Hiển thị nút "THAM GIA NGAY" màu Neon Green (#39FF14).
    - Khi chạm vào: Chuyển sang trạng thái đã tham gia, thay đổi giao diện nút thành "HỦY KÈO CHỜ" màu Destructive Red (#FF3B30) để tạo sự tương phản.
    - Tích hợp Bottom Sheet vào màn hình bản đồ `app/(tabs)/map.tsx`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <acceptance_criteria>
    - MatchBottomSheet bọc trong BottomSheet của gorhom
    - Nút bấm thay đổi màu sắc và văn bản tương ứng giữa trạng thái "THAM GIA" và "HỦY"
  </acceptance_criteria>
  <done>Giao diện bản đồ và luồng Bottom Sheet tương tác hoàn hảo.</done>
</task>
</tasks>

<verification>
- [ ] Chạy lệnh `npx tsc --noEmit` đạt kết quả biên dịch TypeScript thành công.
</verification>

<success_criteria>
- Bản đồ kết nối được tọa độ mock, radar quét nhịp nhàng không lag giật.
- Bottom sheet hỗ trợ tương tác vuốt kéo tự nhiên và nút tham gia/hủy chuyển đổi tức thời.
</success_criteria>

<output>
After completion, create `.planning/phases/COURTMATE-01-foundation-on-demand-matchmaking/01-03-SUMMARY.md`
</output>
