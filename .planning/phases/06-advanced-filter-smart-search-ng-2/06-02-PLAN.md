# 06-02: Frontend Search & Filter UI

<objective>
Implement a debounced search bar and a bottom sheet for advanced filtering on the tournament hub screen.
</objective>

<tasks>
1. Update `apps/frontend/src/features/tournaments/services/tournaments.api.ts` to accept a filter object and serialize it into query string parameters for the `findAll` endpoint.
2. In `TournamentHubScreen.tsx`:
   - Add a state object for filters (keyword, sport, city, status).
   - Implement a Search Bar component at the top of the screen.
   - Implement debouncing (300ms) for the keyword search input before triggering an API call.
   - Add a "Filter" icon button next to the search bar.
   - Create a `FilterBottomSheet` component using Tamagui's Sheet that allows users to select sport types and regions.
   - Ensure the filter UI aligns with the decisions in `06-CONTEXT.md` (debounced typing, sheet UI).
   - When filters are applied from the sheet, close the sheet and trigger a re-fetch of tournaments.
</tasks>
