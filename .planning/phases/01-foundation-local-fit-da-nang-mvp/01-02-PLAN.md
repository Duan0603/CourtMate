---
phase: 01-foundation-local-fit-da-nang-mvp
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/frontend/src/features/auth/services/auth.api.ts
  - apps/frontend/src/features/auth/hooks/useLogin.ts
  - apps/frontend/src/features/auth/screens/LoginScreen.tsx
  - apps/frontend/src/features/auth/screens/OnboardingScreen.tsx
  - apps/frontend/src/App.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "D-08: Store the JWT securely in expo-secure-store on the mobile client"
    - "D-09: Mandate a multi-step onboarding wizard immediately after sign-up"
    - "D-10: Redirect and lock users in the onboarding wizard if they log in but have not finished onboarding"
    - "D-11: Display a multi-select grid/list of supported sports (Badminton, Football, Pickleball, Tennis) with icons"
    - "D-12: Provide simple categorical ratings (Beginner, Intermediate, Advanced) for each selected sport"
    - "D-13: Predefined list of pilot cities (Da Nang, Ha Noi, Ho Chi Minh) via a dropdown/picker"
    - "D-14: Request device GPS location permission during onboarding. If granted, auto-detect the user's city and pre-select it"
    - "D-15: If GPS shows user is outside pilot cities, inform them and prompt to select a pilot city to proceed"
    - "D-16: Allow users to change their primary city in settings/profile settings at any time"
  artifacts:
    - path: "apps/frontend/src/features/auth/screens/OnboardingScreen.tsx"
      provides: "Multi-step onboarding UI (Role -> Details -> Sports/Club)"
    - path: "apps/frontend/src/features/auth/hooks/useLogin.ts"
      provides: "Refactored login flow supporting OTP request/verification and SecureStore saving"
  key_links:
    - from: "apps/frontend/src/features/auth/screens/OnboardingScreen.tsx"
      to: "apps/frontend/src/features/auth/services/auth.api.ts"
      via: "PUT request to save completed profile preferences"
      pattern: "authApi\\.updateProfile"
    - from: "apps/frontend/src/App.tsx"
      to: "apps/frontend/src/features/auth/screens/OnboardingScreen.tsx"
      via: "Redirection check forcing un-onboarded users to Onboarding screen"
      pattern: "OnboardingScreen"
---

<objective>
Update frontend authentication hooks and screens, build the onboarding flow wizard, and implement navigation route protection.

Purpose: Implement the client-side authentication lifecycle, secure storage, and mandatory profile collection.
Output: Login UI with OTP submission, SecureStore service integration, and the Onboarding Screen component.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-fit-da-nang-mvp/01-CONTEXT.md
@apps/frontend/src/features/auth/screens/LoginScreen.tsx
@apps/frontend/src/features/auth/hooks/useLogin.ts
@apps/frontend/src/features/auth/services/auth.api.ts
@apps/frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Auth Service and Hook for Passwordless OTP</name>
  <files>apps/frontend/src/features/auth/services/auth.api.ts, apps/frontend/src/features/auth/hooks/useLogin.ts</files>
  <read_first>apps/frontend/src/features/auth/services/auth.api.ts, apps/frontend/src/features/auth/hooks/useLogin.ts</read_first>
  <action>Refactor auth.api.ts to send requests to backend endpoints: requestOtp(email) and verifyOtp(email, code). Update useLogin.ts to support state machine phases: requestOtp, verifyOtp (which stores the returned token into expo-secure-store), and loading token on mount.</action>
  <verify>Run npm run lint inside apps/frontend</verify>
  <acceptance_criteria>
    - auth.api.ts calls POST /auth/request-otp and POST /auth/verify-otp
    - useLogin.ts uses SecureStore.setItemAsync to persist JWT tokens
    - useLogin.ts exposes isLoading, requestOtp, verifyOtp, and isAuthenticated
  </acceptance_criteria>
  <done>Authentication service and hook updated for OTP validation</done>
</task>

<task type="auto">
  <name>Task 2: Build Login and OTP Submission UI</name>
  <files>apps/frontend/src/features/auth/screens/LoginScreen.tsx</files>
  <read_first>apps/frontend/src/features/auth/screens/LoginScreen.tsx</read_first>
  <action>Refactor LoginScreen.tsx to show a step-by-step layout: First form requests email and submits. Upon response, show a 6-digit verification code input field. Incorporate loading states, error handling feedback, and style using Tamagui.</action>
  <verify>Start frontend and visually check compilation</verify>
  <acceptance_criteria>
    - LoginScreen.tsx handles transitioning from Email input form to Verification Code form
    - LoginScreen.tsx utilizes Tamagui widgets (Input, Button, Spinner, YStack)
  </acceptance_criteria>
  <done>Login and verification code UI implemented</done>
</task>

<task type="auto">
  <name>Task 3: Develop Mandatory Onboarding Wizard Screen</name>
  <files>apps/frontend/src/features/auth/screens/OnboardingScreen.tsx, apps/frontend/src/App.tsx</files>
  <read_first>apps/frontend/src/App.tsx</read_first>
  <action>Create OnboardingScreen.tsx. Step 1: Select Role (PLAYER or ORGANIZER). Step 2: Name and City Selection (dropdown listing Da Nang, Ha Noi, Ho Chi Minh; use expo-location to request GPS permission and pre-select the city). Step 3: Role-specific details (PLAYER selects multiple sports from icons grid and skill levels: Beginner, Intermediate, Advanced; ORGANIZER enters Club/Organization name). Upon submission, send PUT /users/profile request to backend to save profile, set onboarding complete flag. In App.tsx, check if authenticated user lacks onboarding, and redirect them to OnboardingScreen.</action>
  <verify>tsc --noEmit inside apps/frontend succeeds</verify>
  <acceptance_criteria>
    - OnboardingScreen.tsx exists and contains the multi-step form wizard
    - Location step requests GPS permission, defaulting to manual dropdown if denied
    - If user has not finished onboarding, App.tsx redirects them to OnboardingScreen and disables navigation away
  </acceptance_criteria>
  <done>Onboarding wizard and route protection guard fully active</done>
</task>

</tasks>

<verification>
- [ ] tsc --noEmit passes for frontend
- [ ] App launches without javascript errors
- [ ] User onboarding state correctly locks user navigation
</verification>

<success_criteria>
- Mobile client successfully logs in and onboarding completes
- Profile values stored inside backend DB
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-fit-da-nang-mvp/01-02-SUMMARY.md`
</output>
