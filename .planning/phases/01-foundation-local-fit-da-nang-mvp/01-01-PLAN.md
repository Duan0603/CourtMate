---
phase: 01-foundation-local-fit-da-nang-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/index.ts
  - apps/backend/src/app.module.ts
  - apps/backend/src/modules/users/user.schema.ts
  - apps/backend/src/modules/users/users.service.ts
  - apps/backend/src/modules/users/users.controller.ts
  - apps/backend/src/modules/users/users.module.ts
  - apps/backend/src/modules/auth/auth.service.ts
  - apps/backend/src/modules/auth/auth.controller.ts
  - apps/backend/src/modules/auth/auth.module.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - "D-01: Extend the UserRole enum in @courtmate/shared directly with PLAYER and ORGANIZER roles"
    - "D-02: Lock the role permanently at onboarding so users cannot change it later"
    - "D-03: System admins (REGIONAL_ADMIN, SUPER_ADMIN) inherit/extend player/organizer capabilities"
    - "D-04: User schema requires Name, Email, Location for both; sports/skills for PLAYER; clubName for ORGANIZER"
    - "D-05: Implement a real backend JWT auth flow with user schema and persistent MongoDB storage"
    - "D-06: Passwordless/Magic Link auth sending OTP code via email"
    - "D-07: Support a development mode that logs the OTP to the backend console"
  artifacts:
    - path: "packages/shared/src/index.ts"
      provides: "Updated UserRole enum including PLAYER and ORGANIZER"
    - path: "apps/backend/src/modules/users/user.schema.ts"
      provides: "Mongoose schema mapping to User types with role and location validation"
    - path: "apps/backend/src/modules/auth/auth.service.ts"
      provides: "OTP creation, email validation, and JWT signing operations"
  key_links:
    - from: "apps/backend/src/modules/auth/auth.controller.ts"
      to: "apps/backend/src/modules/auth/auth.service.ts"
      via: "Dependency injection for requestOtp and verifyOtp endpoints"
      pattern: "authService\\.(requestOtp|verifyOtp)"
    - from: "apps/backend/src/modules/auth/auth.service.ts"
      to: "apps/backend/src/modules/users/users.service.ts"
      via: "User lookup and creation during OTP verification"
      pattern: "usersService\\.(findByEmail|create)"
---

<objective>
Build backend User schemas, Mongoose database configuration, and NestJS authentication controllers.

Purpose: Provide a persistent user database layer and a passwordless email OTP auth endpoint.
Output: NestJS modules for users and authentication, persistent MongoDB user schema, and JWT guards.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-local-fit-da-nang-mvp/01-CONTEXT.md
@packages/shared/src/index.ts
@apps/backend/src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UserRole enum in shared package</name>
  <files>packages/shared/src/index.ts</files>
  <read_first>packages/shared/src/index.ts</read_first>
  <action>Open packages/shared/src/index.ts. Add PLAYER = 'PLAYER' and ORGANIZER = 'ORGANIZER' to the UserRole enum, alongside existing USER, REGIONAL_ADMIN, and SUPER_ADMIN. Build the shared package to verify types compilation.</action>
  <verify>Run npm run build inside packages/shared</verify>
  <acceptance_criteria>
    - packages/shared/src/index.ts contains "PLAYER = 'PLAYER'"
    - packages/shared/src/index.ts contains "ORGANIZER = 'ORGANIZER'"
    - Compilation succeeds without typescript errors
  </acceptance_criteria>
  <done>UserRole enum successfully extended and compiled</done>
</task>

<task type="auto">
  <name>Task 2: Implement Users Module and Mongoose Schema</name>
  <files>apps/backend/src/modules/users/user.schema.ts, apps/backend/src/modules/users/users.service.ts, apps/backend/src/modules/users/users.controller.ts, apps/backend/src/modules/users/users.module.ts</files>
  <read_first>apps/backend/src/app.module.ts, packages/shared/src/index.ts</read_first>
  <action>Create user.schema.ts with Mongoose schema. Fields: email (unique string), name (string), role (string enum: PLAYER, ORGANIZER, REGIONAL_ADMIN, SUPER_ADMIN), preferences (sports array of SportType, skillLevel string, clubName string, location string). Implement UsersService for findByEmail, create, and updateProfile, and link the module in apps/backend/src/app.module.ts.</action>
  <verify>tsc --noEmit inside apps/backend passes</verify>
  <acceptance_criteria>
    - user.schema.ts exists and defines UserSchema mapping to the shared User type
    - app.module.ts imports and registers UsersModule
    - UsersService implements create and findByEmail with Mongoose User model injection
  </acceptance_criteria>
  <done>Users module registered and MongoDB collection schema active</done>
</task>

<task type="auto">
  <name>Task 3: Build Passwordless Auth Module with JWT</name>
  <files>apps/backend/src/modules/auth/auth.service.ts, apps/backend/src/modules/auth/auth.controller.ts, apps/backend/src/modules/auth/auth.module.ts</files>
  <read_first>apps/backend/src/app.module.ts, apps/backend/src/modules/users/users.service.ts</read_first>
  <action>Create AuthModule. Implement AuthService to generate 6-digit OTP, store it in an in-memory map (or Redis if configured, fallback to memory), and verify it. Implement routes: POST /auth/request-otp (body: email; checks email, logs code to console with header '[DEV ONLY] OTP code for email is XXXXXX'), POST /auth/verify-otp (body: email, code; verifies, looks up/creates User with default role PLAYER, signs and returns JWT). Implement a JwtStrategy and Route Guard.</action>
  <verify>Run npm run build inside apps/backend</verify>
  <acceptance_criteria>
    - auth.controller.ts has POST endpoints for request-otp and verify-otp
    - auth.service.ts uses @nestjs/jwt to sign payloads
    - Requesting OTP prints the dev console banner with the 6-digit code
  </acceptance_criteria>
  <done>Authentication endpoints active and issuing JWT tokens</done>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds for backend
- [ ] API endpoints respond with 200/201 on valid OTP lifecycle
- [ ] User record created in MongoDB matches preferences provided
</verification>

<success_criteria>
- Backend auth endpoints verify successfully
- DB records store role and location constraints
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-local-fit-da-nang-mvp/01-01-SUMMARY.md`
</output>
