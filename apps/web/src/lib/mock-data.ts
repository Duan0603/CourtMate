import { Tournament, User, Registration } from '@courtmate/shared';

// No hardcoded mock data - all entities are fetched from backend APIs
export const MOCK_TOURNAMENTS: Tournament[] = [];
export const MOCK_USER: User | null = null;
export const MOCK_REGISTRATIONS: Registration[] = [];
